{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "about",
  "description": "Narrow capability for the standalone About window. Only allows reading the about-info IPC and opening the website URL in the system browser. Excludes window manipulation, global shortcuts, notifications, and deep-linking — none of which the About page needs.",
  "windows": ["about"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
